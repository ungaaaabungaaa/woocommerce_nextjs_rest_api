import React from "react";

// form validation
// create customer using the UID from authenticated user
// make the reverse api to check and validate the data exist or not
// if uid and coustomer id exist then show edit options
// if uid and not customer id exist show update details
// add in universal check after login if the profile data is missing send them here to collect the details
// roboust testing
// add in auth hook to check if the uesr is logged to show diffrent icons on the navbar

function Profile() {
  return (
    <div>
      <h1>Profile</h1>
    </div>
  );
}

export default Profile;
